# This is the configuration file for Ghostty.
#

# I moved this file from `/Users/daniel/Library/Application Support/com.mitchellh.ghostty/config`
# to GNU stow managed `~/.config/ghostty/config`
# if **XDG_CONFIG_HOME** is not set, both are loaded if they exist

# DL-2026-03-13: Overrode and moved to GNU Stow
# This template file has been automatically created at the following
# path since Ghostty couldn't find any existing config files on your system:
#
#   /Users/daniel/Library/Application Support/com.mitchellh.ghostty/config
#
# The template does not set any default options, since Ghostty ships
# with sensible defaults for all options. Users should only need to set
# options that they want to change from the default.
#
# Run `ghostty +show-config --default --docs` to view a list of
# all available config options and their default values.
#
# Additionally, each config option is also explained in detail
# on Ghostty's website, at https://ghostty.org/docs/config.
#
# Some config options have unique syntaxes for their value,
# which is explained in the docs for that config option.
# Just for example:
# resize-overlay-duration = 4s 200ms
#
# Shift+Enter sends ESC+CR, allowing multi-line input in apps like Claude Code
keybind = shift+enter=text:\x1b\r
# 2026-02-20 - avoid needless confirmations
clipboard-paste-protection = false
# DL-2026-03-13: Looking for a workable workflow to preserve current work,
# which is tightly coupled to open shells.
window-save-state = always

# DL-2026-03-13: Fix TERM compatibility when SSHing to remote hosts
# Remote servers typically don't have xterm-ghostty terminfo installed.
# ssh-env auto-converts TERM to xterm-256color and propagates COLORTERM, etc.
# Defaults: cursor, no-sudo, title, no-ssh-env, no-ssh-terminfo, path
# We enable: ssh-env (fix SSH), sudo (preserve terminfo with sudo)
shell-integration-features = cursor,sudo,title,ssh-env
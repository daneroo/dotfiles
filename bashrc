# echo "*** now executing empty .bashrc"
# This should be empty 
# everything goes into .bash_profile
# 2023-11-17 - no longer conditional on $PS1
# [ -n "$PS1" ] && source ~/.bash_profile;
source ~/.bash_profile;
